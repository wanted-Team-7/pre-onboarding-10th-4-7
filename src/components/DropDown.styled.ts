import styled from 'styled-components';

const Container = styled.div`
  position: absolute;
  width: 100%;
  height: 200px;
  overflow-y: scroll;
  background-color: aquamarine;
`;

export const S = {
  Container,
};
